package org.example.behavioural_design_pattern.Observer;

// Observer interface with the update method.
public interface Observer {
    // Method to update the observer with new temperature data.
    void update(float temperature);
}
